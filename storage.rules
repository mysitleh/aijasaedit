rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Memberikan akses publik baca dan tulis (Untuk tahap development / project public)
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
